<?php

namespace app\models;

use Yii;

/**
 * This is the model class for table "mavito".
 *
 * @property string $id
 * @property string $addr
 * @property string $price
 * @property string $m2
 * @property string $floor
 * @property string $links
 * @property string $descr
 * @property string $date
 * @property string $last_updated
 * @property string $tel
 */
class Mavito extends \yii\db\ActiveRecord
{
    /**
     * @inheritdoc
     */
    public static function tableName()
    {
        return 'mavito';
    }

    /**
     * @inheritdoc
     */
    public function rules()
    {
        return [
            [['id'], 'required'],
            [['addr', 'descr', 'last_updated'], 'string'],
            [['date'], 'safe'],
            [['id'], 'string', 'max' => 12],
            [['price', 'floor'], 'string', 'max' => 20],
            [['m2'], 'string', 'max' => 30],
            [['links'], 'string', 'max' => 80],
            [['tel'], 'string', 'max' => 15],
            [['id'], 'unique'],
        ];
    }

    /**
     * @inheritdoc
     */
    public function attributeLabels()
    {
        return [
            'id' => Yii::t('app', 'ID'),
            'addr' => Yii::t('app', 'Адрес'),
            'price' => Yii::t('app', 'Цена'),
            'm2' => Yii::t('app', 'Площадь'),
            'floor' => Yii::t('app', 'Этаж'),
            'links' => Yii::t('app', 'Ссылка'),
            'descr' => Yii::t('app', 'Описание'),
            'date' => Yii::t('app', 'Записано'),
            'last_updated' => Yii::t('app', 'Опубликовано'),
            'tel' => Yii::t('app', 'Телефон'),
        ];
    }

    /**
     * @inheritdoc
     * @return MavitoQuery the active query used by this AR class.
     */
    public static function find()
    {
        return new MavitoQuery(get_called_class());
    }
}
